import React from "react";

export const MemoInput = () => {
  return (
    <div className="md:w-2/3 w-full">
      {/* <%= form_with(model: @memo, className: "contents") do |form| %>
      <% if @memo.errors.any? %>
        <div id="error_explanation" className="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
          <h2><%= pluralize(@memo.errors.count, "error") %> prohibited this memo from being saved:</h2>

          <ul>
            <% @memo.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div className="my-8">
        <div className="flex mt-4">
          <%= form.text_field :body, className: "block shadow rounded-md border border-gray-200 outline-none mr-4 px-3 py-2 w-full" %>
          <%= form.submit className: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
        </div>
      </div>
      
          
    <% end %> */}
    </div>
  );
};
